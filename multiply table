#include <stdio.h>
int main ()
{	
	int n,i,j;
	printf("Enter the number for the multiplication table : ");
	scanf("%d",&n);
	printf("Enter the number of rows : ");
	scanf("%d",&j);
	for  (i=1;i<=j;i++)
	{
		printf("%d X %d = %d \n",n,i,n*i);
	}
	return 0;
}
